# DataGandalf
Data Gandalf is a SAS-sponsored dataset discovery app that searches and ranks public datasets (from sources like Kaggle) using OpenSearch and semantic similarity. Users can query or filter by topic and open a dataset’s metadata, tags, licenses, and other details.
